- CRITICAL — `packages/agents/tools/evolution_tools.py:610-618`, `packages/agents/tools/evolution_tools.py:643-645`: The agent-facing infra evolution tool fails open when `CEDAR_POLICY_STORE_ID` is missing or AVP errors. That directly violates the vision that autonomous infra edits are bounded by Cedar, and a prod misconfiguration becomes an allow-all path. Fix by fail-closing outside explicit local/dev mode and requiring policy-store health before commit.

- CRITICAL — `packages/agents/tools/evolution_tools.py:153-182`, `packages/agents/tools/evolution_tools.py:789-803`, `packages/agents/tools/evolution_tools.py:876-877`: The tool commits CDK to CodeCommit `main` before the audit record, and audit write failure is only logged. This creates a real path where an evolution decision triggers pipeline deployment with no durable audit trail. Fix by creating a pending audit/idempotency record before CodeCommit, conditionally marking committed after success, and failing closed on audit-write failure.

- HIGH — `packages/agents/tools/evolution_tools.py:100-107`, `packages/agents/tools/evolution_tools.py:156-169`, `packages/agents/tools/evolution_tools.py:763-767`: An agent can submit arbitrary stack code to a user-selectable repo/region and only weak substring validation blocks it. The blast radius of prompt-induced misuse is production infra mutation through the deploy pipeline. Fix with repo/path allowlists, PR-only writes, AST/CDK synth validation before commit, scoped execution role, and mandatory HITL for non-additive changes.

- HIGH — `packages/core/src/evolution/self-evolution-orchestrator.ts:173-180`, `packages/core/src/evolution/self-evolution-orchestrator.ts:210-240`, `packages/core/src/evolution/self-evolution-orchestrator.ts:493-496`: The TypeScript orchestrator authorizes with `estimatedMonthlyCostDelta: 0`, then commits generated code without re-authorizing actual `genResult.estimatedCostDelta`, and later swallows audit failures. Cost/HITL Cedar policy can be bypassed by underestimating pre-generation cost. Fix by re-running Cedar after generation, before commit, with actual resources/cost and fail-closed audit.

- HIGH — `packages/core/src/evolution/auto-skill-gen.ts:186-213`, `packages/core/src/evolution/auto-skill-gen.ts:218-267`: Auto-skills can be "tested" with random pass/fail simulation and published straight to S3/DDB with `status: 'published'`. That bypasses the advertised 7-stage security pipeline and Cedar trust gating. Fix by routing generated skills through the existing scanner/deployer pipeline and recording approval lineage.

- HIGH — `packages/agents/chimera_agent.py:47-60`, `packages/agents/chimera_agent.py:254-258`, `packages/agents/chimera_agent.py:339-348`: Memory namespace construction trusts raw `tenantId`/`userId` claims and interpolates them without validation; missing `userId` becomes `user-None`, and slash/control characters could corrupt namespace shape. Claim tampering should be blocked upstream, but this code has no defense-in-depth. Fix by requiring verified `custom:tenant_id`/`sub`, strict charset validation, and rejecting absent user/session IDs.

- HIGH — `packages/core/src/memory/client.ts:102-108`, `packages/core/src/memory/client.ts:121-122`: The production AgentCore memory client is unimplemented, and the only factory that returns a client accepts an arbitrary namespace string. The TypeScript stub is therefore not tenant-scoped in any enforceable way. Fix by exposing factories that take `{tenantId,userId,sessionId}` and internally call canonical namespace builders.

- MED — `packages/core/src/swarm/task-decomposer.ts:56-64`, `packages/core/src/swarm/task-decomposer.ts:304-312`, `packages/core/src/swarm/task-decomposer.ts:353-360`, `packages/core/src/swarm/task-decomposer.ts:441-448`: Phase-2 decomposition still has production TODOs and silently falls back to generic heuristic plans when no LLM provider exists or the provider fails. This is acceptable for dev, but production-dangerous because vague infra/security work becomes executable-looking subtasks. Fix by gating heuristic fallback to dev or requiring human approval/degraded status in prod.

- HIGH — `infra/lambdas/schedule-dispatcher/index.py:203-239`, `infra/lambdas/schedule-dispatcher/index.py:246-263`, `infra/lambdas/schedule-dispatcher/index.py:493-499`: Stuck-RUNNING takeover is not idempotent because `_finalize_run` does not condition on `currentRunId`. A slow old invocation can overwrite the status of a newer recovered run. Fix by passing `run_id` into `_finalize_run` and conditionally updating only when `currentRunId = :rid`.

- INFO — `infra/lambdas/schedule-dispatcher/index.py:105-115`, `infra/lambdas/schedule-dispatcher/index.py:448-463`: The dispatcher correctly signs `tenantId:scheduleId:timestamp:sha256(body)` over the exact serialized body bytes, so replay with a modified body should fail if the chat-gateway verifier mirrors this algorithm. Add a verifier-side regression test proving header/body mismatch is rejected.

- HIGH — `packages/agents/chimera_agent.py:97-137`, `packages/agents/observability.py:61-80`: The Python AgentCore path emits only a best-effort EMF `loop_iterations` metric and silently drops metric failures; there is no evidence of GenAI semantic attributes such as `gen_ai.system_instructions`, `gen_ai.tool.calls`, or token usage. Fix by adding OpenTelemetry spans around model/tool calls with GenAI semantic-convention attributes and fail-visible exporter health metrics.
