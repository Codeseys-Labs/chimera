- CRITICAL — `infra/lib/tenant-onboarding-stack.ts:364` / `infra/lib/data-stack.ts:295`: generated tenant IAM roles grant S3 access to `chimera-tenants-${envName}` and `chimera-skills-${envName}` while the real buckets include account+region. This breaks the golden tenant role path and can push operators toward broader emergency grants. Fix by passing bucket ARNs/names into onboarding and generating policies from actual constructs.
- HIGH — `infra/lib/tenant-onboarding-stack.ts:341` and `infra/constructs/tenant-agent.ts:108`: tenant roles first allow broad read/write across all Chimera DDB tables, then rely on a DENY with `dynamodb:LeadingKeys`. The vision wants structural tenant isolation at IAM; deny-after-allow is fragile and incompatible with non-tenant GSIs/catalog tables. Fix with positive `LeadingKeys` conditions per table/index, and separate shared catalog read paths.
- HIGH — `infra/lib/pipeline-stack.ts:750` / `infra/lib/pipeline-stack.ts:965` / `infra/lib/orchestration-stack.ts:683`: several `resources: ['*']` are wider than service limitations require, especially ECS `UpdateService`/task-definition actions and Glue catalog reads. This lets autonomous rollout/data workflows affect unrelated resources. Scope to named cluster/service/task definition and Glue catalog/database/table ARNs; keep wildcard only for documented global APIs.
- HIGH — `infra/lib/chat-stack.ts:438` and `infra/lib/chat-stack.ts:451`: shared chat task role can create/invoke/delete all AgentCore code-interpreter and browser sessions account-wide. That is a tenant escape hatch if the gateway is compromised. Add tenant/session tags or condition keys when supported, otherwise broker these actions through a tenant-authorizing Lambda.
- HIGH — `infra/lib/data-stack.ts:12` / `infra/lib/chat-stack.ts:33` / `infra/lib/chat-stack.ts:148`: DataStack and ChatStack mutually accept and mutate each other’s security groups for DAX ingress. This is a cross-stack cycle trap and obscures ownership. Put the chat task SG in NetworkStack or a dedicated SharedSecurityStack and let one stack own ingress rules.
- MED — `infra/lib/orchestration-stack.ts:399`: OrchestrationStack hardcodes DataStack table names instead of receiving table constructs. This creates hidden coupling and bypasses grant helpers/KMS relationships. Pass `tenantsTable`/`sessionsTable` props and grant explicitly.
- MED — `infra/constructs/chimera-lambda.ts:59`, `infra/constructs/chimera-queue.ts:42`, `infra/constructs/chimera-table.ts:67`: construct-created CMKs do not consistently inherit env removal policy; table CMKs always RETAIN, while Lambda/Queue CMKs use the CDK default. Dev destroys will leave keys or behave inconsistently. Add explicit `removalPolicy` props and thread env policy through all constructs.
- MED — `infra/lib/data-stack.ts:294` and `infra/lib/data-stack.ts:321`: dev S3 buckets use `RemovalPolicy.DESTROY` but omit `autoDeleteObjects`, so non-empty tenant/skills buckets and their access-log buckets can block destroy. Set `autoDeleteObjects: !isProd` where destroy is requested, or make RETAIN intentional.
- MED — `infra/lib/chat-stack.ts:722` / `infra/lib/network-stack.ts:34` / `infra/lib/data-stack.ts:264`: non-prod still runs ECS desiredCount=1, one NAT Gateway, and one DAX node. That is not aligned with cost-gated autonomous infra. Add context/env gates for zero-scale dev, NAT-less isolated paths, and optional DAX.
- MED — `infra/lib/email-stack.ts:260`, `infra/lib/pipeline-stack.ts:625`, `infra/lib/tenant-onboarding-stack.ts:102`: many Lambdas bypass `ChimeraLambda`, losing mandatory DLQ/X-Ray defaults; several hand-created log groups also omit CMK encryption, e.g. `infra/lib/chat-stack.ts:171` and `infra/lib/email-stack.ts:254`. Standardize on `ChimeraLambda` or equivalent props for tracing, DLQ, retention, and encrypted logs.
- LOW — `infra/lib/registry-stack.ts:10` and `infra/lib/api-stack.ts:251`: Registry is still a placeholder while API/skill deployment grants wildcard AgentCore registry search/write. This is a vision gap for tenant-scoped skill marketplace. Finish registry tenancy design before enabling write paths broadly.
