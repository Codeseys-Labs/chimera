- CRITICAL [packages/cli/src/commands/diff.ts:54, packages/cli/src/commands/diff.ts:68] `chimera diff` swallows all CodeCommit `GetFile/GetFolder` errors, so `AccessDeniedException` can be misreported as “folder missing” or empty remote state with exit 0. This defeats IAM diagnosability and can make operators trust a false diff. Only ignore explicit not-found errors; surface permission/network errors and return JSON/nonzero.
- CRITICAL [packages/cli/src/commands/destroy.ts:627, packages/cli/src/commands/destroy.ts:635] fallback stack deletion failures are logged and ignored, then the command continues to delete the pipeline/repo and can print success. A partial destroy can strand app stacks while removing the easiest recovery path. Accumulate failed stack names and exit nonzero unless every fallback delete succeeds.
- HIGH [packages/cli/src/cli.ts:56] command coverage still falls short of the “one interface” vision for scheduled invocations/EventBridge Scheduler, channel integrations, skill scan/review pipeline, self-evolution experiments/model routing, tenant quotas/cost/audit/Cedar inspection, and CloudWatch dashboard/alarm operations. Those workflows still push operators toward raw `aws scheduler`, `aws events`, `aws dynamodb`, `aws cloudwatch`, or console work. Add first-class `schedule`, `integration`, `evolution`, `quota/cost`, `audit`, and `alarm/dashboard` commands.
- HIGH [packages/cli/src/commands/deploy.ts:696, packages/cli/src/commands/deploy.ts:700] `chimera deploy` does not check whether CodePipeline is already `InProgress` before pushing a new CodeCommit batch, and it reports success once the source is pushed. Mid-run deployments can queue/collide while the CLI says “Deployment complete.” Query latest pipeline execution before push, offer `--wait/--force`, and include pipeline execution id/status in JSON.
- HIGH [packages/cli/src/commands/destroy.ts:500] `chimera destroy` returns “Nothing to destroy” when local `chimera.toml` lacks `deployment`, without scanning AWS for `Chimera-{env}-*` stacks. A previous partial destroy or config loss becomes silent success with live resources. Always perform an AWS stack/repo discovery pass unless `--local-only` is explicit.
- MED [packages/cli/src/commands/diff.ts:105, packages/cli/src/commands/trigger.ts:12, packages/cli/src/commands/completion.ts:216] `--json` parity gaps are `diff`, `trigger`, and `completion`; most other command surfaces do support JSON: deploy/destroy/cleanup/redeploy/status/monitor/logs/ecs/tail-pipeline/rollback/doctor/predeploy/init/login/setup/sync/endpoints/tenant/session/skill/chat-message/upgrade. Missing JSON blocks scripts from uniformly consuming outcomes. Add `{status,data|error,code}` envelopes and tests for these three.
- MED [packages/cli/src/utils/pipeline-logs.ts:276] `buildFailedStageReport` silently ignores `GetPipelineExecution` failure, including IAM denial, then infers status from action history. Users may see plausible but degraded status with no permission hint. Preserve the warning in the report and stderr/JSON.
- MED [packages/cli/src/commands/deploy.ts:745, packages/cli/src/commands/deploy.ts:783] endpoint collection and admin setup catch errors and downgrade them to warnings. That is acceptable for eventual pipeline completion, but IAM failures are not classified, so a real permission gap looks like ordinary “try later.” Detect AWS error names and print targeted permission/action guidance.
- MED [packages/cli/src/commands/logs.ts:127, packages/cli/src/commands/ecs.ts:302, packages/cli/src/commands/trigger.ts:12, packages/cli/src/commands/diff.ts:105, packages/cli/src/commands/destroy.ts:713, packages/cli/src/commands/destroy.ts:774] help text is uneven: Commander provides `--help`, but logs/ecs/trigger/diff/cleanup/redeploy lack examples, while deploy help misleadingly says “triggers pipeline” though it relies on CodeCommit auto-trigger. Add examples and correct the deploy wording.
- INFO [packages/cli/src/__tests__/commands/ecs.test.ts:4, packages/cli/src/__tests__/commands/tail-pipeline.test.ts:6] command testability is partial: many tests cover Commander wiring/pure helpers while explicitly avoiding AWS SDK I/O, and command actions instantiate SDK clients internally. This makes failure semantics hard to unit-test. Factor AWS clients behind injectable runners and add mocked action-level tests for IAM denial, in-progress pipeline, and partial destroy.
